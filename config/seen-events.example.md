# Seen Events Log

> Auto-maintained by the system. Every event surfaced in a report is logged here
> so it won't appear again the following week — unless something new warrants it.
>
> **Re-include an event only if:**
> - Status is — (undecided) AND a registration/early-bird deadline is within 48h
> - An explicit re-include trigger is set in the trigger column below
>
> The system cleans up entries for events whose date has already passed.

---

| Event name | Event date | First shown | Status | Re-include trigger |
|------------|------------|-------------|--------|-------------------|
